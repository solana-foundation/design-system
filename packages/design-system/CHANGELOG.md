# @solana/design-system

## 1.0.1

### Patch Changes

- Fix Storybook tooling resolution and ensure `dev` builds workspace dependencies so subpath exports like `@solana/design-system/button` work in local fixtures.
